#ifndef RADIORANDOM_HELPERS_FORMAT_USERACCESS
#define RADIORANDOM_HELPERS_FORMAT_USERACCESS

/// @file

#include <string>

using namespace std;

/// Formats access level to human-readable format
///
/// \param [in] level of user
/// @return string represing access level
string format_access(int level);

#endif
