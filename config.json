{
	"db-schema" : {
		"users" : "CREATE TABLE users (username TEXT, password_hash TEXT, score INTEGER, access INTEGER, joined DATE );",
		"files" : "CREATE TABLE files (filename TEXT, filepath TEXT, hash TEXT, size INTEGER, score INTEGER, posted DATE);"
	},
	"setup" : {
		"lockfile" : "/home/http/radiorandom/setuplock"
	},
	"http" : { 
		"script" : "bska" 
	},
	"service" : {
		"api" : "fastcgi",
		"socket" : 8080
	},
	"daemon" : {
		"enable" : false,
		"lock" : "/var/run/radiorandom.pid"
	}
}
